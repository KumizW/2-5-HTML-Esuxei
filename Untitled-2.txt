//das13
//das1
#include <stdio.h>
int urt;
void talbai(int talbai)
{
    talbai=urt*urt;
    printf("talbai=%d",talbai);
    
}
int main() {
    printf("taliin urtiig oruul: ");scanf("%d",&urt);
    talbai(urt);
    return 0;
}
//das2 
#include <stdio.h>
    void check(int too)
    {
        if(too%2 == 0)
        printf("tegsh too");
        else
        printf("sondgoi too");
    }
int main() {
    int too;
    printf("too oruul: ");scanf("%d",&too);
    check(too);
    return 0;
}
//das3
#include <stdio.h>
void checkPrime(int too)
{
        if(too<=1)
    {
        printf("anxnii too bish");
        return;
    }
   for(int i=2;i<=too;i++)
    {
        if (too%i==0) 
        {
            printf("anxnii too bish\n");
            return;
        }
    }
    printf("anxnii too mun\n");
}

int main() {
    int too;
    printf("toonii utgiig oruul: ");scanf("%d",&too);
    checkPrime(too);
    return 0;
}
//das4
#include <stdio.h>
int i,a;
void max(int arr1[])
{
  a=arr1[0];
  for(i=0;i<=9;i++)
  {
      if(arr1[i]>a)
      {
          a=arr1[i];
      }
  }
  printf("hamgiin ix:%d",a);
}
int main() {
    int arr1[10]={};
    for(i=0;i<=9;i++)
    {
    printf("arr1[%d]=",i);scanf("%d",&arr1[i]);
    }
    max(arr1);
    return 0;
}